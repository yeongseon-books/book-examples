# sqlalchemy-101 예제 코드

에피소드별 SQLAlchemy 2.x 실행 예제와 pytest 테스트 모음입니다.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```
