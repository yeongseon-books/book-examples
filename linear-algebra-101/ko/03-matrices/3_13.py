# 단순 그래프 인접 행렬
# 0 -> 1, 1 -> 2, 2 -> 0 의 방향 그래프
A = np.array([[0, 1, 0],
              [0, 0, 1],
              [1, 0, 0]])

A2 = A @ A
print('A (1-hop):', A)
print('A^2 (2-hop):', A2)

# A^2[i, j] = A에서 i에서 j로 가는 길이 2 경로 수
