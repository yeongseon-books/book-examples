# software-design-101

소프트웨어 설계 101 시리즈의 에피소드별 파이썬 예제 코드입니다.

- `ko/`, `en/`: 에피소드 01~10 예제 (구조는 동일, 설명 문자열만 언어별)
- `tests/`: 에피소드별 pytest 테스트

실행:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest -q
```
