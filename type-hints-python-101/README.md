# type-hints-python-101 예제 코드

이 저장소는 `type-hints-python-101` 시리즈의 에피소드별 실행 가능한 파이썬 예제를 담고 있습니다.

- `ko/`: 한국어 버전 예제
- `en/`: 영어 버전 예제
- `tests/`: 에피소드별 런타임 테스트

실행:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest -q
```
