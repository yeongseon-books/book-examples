# python-package-101 예제 코드

python-package-101 시리즈의 에피소드별 실행 예제 모음입니다.

- `ko/`: 한국어 글 기준 예제 스크립트
- `en/`: 영어 글 기준 예제 스크립트
- `tests/`: 에피소드별 pytest 테스트

로컬 실행:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest -q
```
