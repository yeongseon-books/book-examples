name: Trigger Demo
on:
  push:
    branches: [main]
    paths:
      - "src/**"
  pull_request:
    branches: [main]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo triggered
