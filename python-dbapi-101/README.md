# python-dbapi-101 예제 코드

이 저장소는 `python-dbapi-101` 시리즈(EP01~EP10)의 실행 가능한 파이썬 예제 모음입니다.

- `ko/`: 한국어 에피소드별 스크립트
- `en/`: 영어 에피소드별 스크립트
- `tests/`: 에피소드별 pytest 테스트

빠른 실행:

```bash
pip install -r requirements.txt
pytest -q
```
