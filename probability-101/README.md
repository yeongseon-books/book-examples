# probability-101 예제 코드

`probability-101` 시리즈(EP01~EP10)의 파이썬 실행 예제 저장소입니다.

- `ko/`: 한국어 버전 에피소드별 코드
- `en/`: 영어 버전 에피소드별 코드
- `tests/`: 에피소드별 pytest 테스트

## 실행

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest -q
```
